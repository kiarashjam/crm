# Backend API base URL (no trailing slash). When set, the frontend uses the real API.
# Example: http://localhost:5160
VITE_API_URL=

# OpenAI API Key for Intelligent Sales Writer (optional - templates work without it)
# Get your key at: https://platform.openai.com/api-keys
# For local development, set this in your environment or use dotnet user-secrets
# OPENAI_API_KEY=sk-proj-your-key-here
