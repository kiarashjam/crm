# Marketing Website (`website/`)

This is a **separate Vite + React app** used for the public-facing/marketing site.

## What’s inside
- `index.html`: entry page
- `src/`: React app code (layouts + public pages)
- `vite.config.ts`: website build/dev config

## How it relates to the CRM app
- The **main CRM product UI** lives in the repo root `src/`.
- This `website/` app is for marketing/public pages and can be deployed separately.

