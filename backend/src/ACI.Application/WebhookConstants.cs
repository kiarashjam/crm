namespace ACI.Application;

/// <summary>Defaults for inbound lead webhooks.</summary>
public static class WebhookConstants
{
    /// <summary>Used when no custom password is stored on the organization.</summary>
    public const string DefaultWebhookPassword = "1234";
}
